<view class="card flex align-center justify-between">
<view class="left">
<view class="code">{{code}}</view>
<view class="post">{{name}}</view>
</view>
<view class="right" bindtap="detail">{{button}}</view>
</view>
<slot></slot>